x-logging: &default-logging
  options:
    max-size: "10m"
  driver: json-file

services:
  app:
    build:
      context: ./docker/nginx
      dockerfile: Dockerfile
      args:
        - UID=${UID:-1000}
        - GID=${GID:-1000}
    expose:
      - 80
    volumes:
      - ./:/var/www/html:delegated
      - ./analytic/thumbnails:/var/www/html/public/analytic-storage/thumbnails
      - ./grz/events:/var/www/html/public/grz-events
    environment:
      TZ: "Europe/Moscow"
      NGINX_CLIENT_MAX_BODY_SIZE: ${NGINX_CLIENT_MAX_BODY_SIZE:-20M}
    depends_on:
      - php
    networks:
      - laravel
      - traefik-public
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik-public"
      - "traefik.http.routers.${TRAEFIK_SERVICE}.rule=Host(`${DOCKER_DOMAIN}`)"
      - "traefik.http.routers.${TRAEFIK_SERVICE}.entrypoints=web"
      - "traefik.http.middlewares.https_redirect.redirectscheme.scheme=https"
      - "traefik.http.routers.${TRAEFIK_SERVICE}.middlewares=https_redirect"
      - "traefik.http.routers.${TRAEFIK_SERVICE}-https.entrypoints=websecure"
      - "traefik.http.routers.${TRAEFIK_SERVICE}-https.rule=Host(`${DOCKER_DOMAIN}`)"
      - "traefik.http.routers.${TRAEFIK_SERVICE}-https.tls=true"
      - "traefik.http.routers.${TRAEFIK_SERVICE}-https.tls.certresolver=le"
    logging: *default-logging

  app-database:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: "${DB_USERNAME}"
      POSTGRES_PASSWORD: "${DB_PASSWORD}"
      POSTGRES_DB: "${DB_DATABASE}"
      TZ: "Europe/Moscow"
    volumes:
      - app_database:/var/lib/postgresql/data:delegated
    networks:
      - laravel
    logging: *default-logging

  php:
    build:
      context: ./docker/php
      dockerfile: Dockerfile
      target: app
      args:
        - UID=${UID:-1000}
        - GID=${GID:-1000}
    expose:
      - 9000
    volumes:
      - ./:/var/www/html:delegated
    depends_on:
      - app-database
      - redis
      - ofelia
    environment:
      TZ: Europe/Moscow
      UPLOAD_MAX_FILESIZE: 5M
      POST_MAX_SIZE: 5M
      MEMORY_LIMIT: 256M
      MAX_EXECUTION_TIME: 300
      MAX_INPUT_TIME: 300
    labels:
      - "ofelia.enabled=true"
      - "ofelia.job-exec.drive-check.schedule=@every 1m"
      - "ofelia.job-exec.drive-check.command=php /var/www/html/artisan drive:check"
      - "ofelia.job-exec.demo-user.schedule=@daily"
      - "ofelia.job-exec.demo-user.command=php /var/www/html/artisan demo:manage-demo-user"
    networks:
      - laravel
      - traefik-public
    logging: *default-logging

  redis:
    image: redis:alpine
    restart: unless-stopped
    volumes:
      - redis_data:/data:delegated
    expose:
      - 6379
    environment:
      TZ: Europe/Moscow
    networks:
      - laravel
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      retries: 3
      timeout: 5s
    logging: *default-logging

  composer:
    build:
      context: ./docker/php
      dockerfile: Dockerfile
      target: app
      args:
        - UID=${UID:-1000}
        - GID=${GID:-1000}
    volumes:
      - ./:/var/www/html:delegated
      - ../packages/:/var/www/packages:delegated
      - ~/.ssh/:/home/laravel/.ssh
    entrypoint: [ "composer" ]
    environment:
      TZ: Europe/Moscow
    networks:
      - laravel
    logging: *default-logging

  npm:
    image: node:current-alpine
    volumes:
      - ./:/var/www/html
    working_dir: /var/www/html
    entrypoint: [ "npm" ]
    command: [ "run", "dev" ]
    environment:
      TZ: Europe/Moscow
    networks:
      - laravel
    logging: *default-logging

  artisan:
    build:
      context: ./docker/php
      dockerfile: Dockerfile
      target: app
      args:
        - UID=${UID:-1000}
        - GID=${GID:-1000}
    volumes:
      - ./:/var/www/html:delegated
    depends_on:
      - app-database
      - redis
    entrypoint: [ "php", "/var/www/html/artisan" ]
    environment:
      TZ: Europe/Moscow
    networks:
      - laravel
    logging: *default-logging

  mailhog:
    image: mailhog/mailhog:latest
    expose:
      - 1025
      - 8025
    environment:
      TZ: Europe/Moscow
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik-public"
      - "traefik.http.routers.${TRAEFIK_SERVICE}-mailhog.rule=Host(`${DOCKER_DOMAIN}`) && Path(`/mailhog`)"
      - "traefik.http.services.${TRAEFIK_SERVICE}-mailhog.loadbalancer.server.port=8025"
      - "traefik.http.routers.${TRAEFIK_SERVICE}-mailhog.service=${TRAEFIK_SERVICE}-mailhog"
      - "traefik.http.routers.${TRAEFIK_SERVICE}-mailhog.entrypoints=websecure"
      - "traefik.http.routers.${TRAEFIK_SERVICE}-mailhog.tls=true"
      - "traefik.http.routers.${TRAEFIK_SERVICE}-mailhog.tls.certresolver=le"
    networks:
      - laravel
    logging: *default-logging

  horizon:
    build:
      context: ./docker/php
      dockerfile: Dockerfile
      target: horizon
      args:
        - UID=${UID:-1000}
        - GID=${GID:-1000}
    volumes:
      - ./:/var/www/html:delegated
      - ./docker/php/supervisord.d/horizon.conf:/etc/supervisord.d/horizon.conf
    depends_on:
      - app-database
      - redis
    environment:
      TZ: Europe/Moscow
    networks:
      - laravel
    logging: *default-logging

  adminer:
    image: adminer
    restart: unless-stopped
    expose:
      - 8080
    depends_on:
      - app-database
    networks:
      - laravel
      - traefik-public
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik-public"
      - "traefik.http.routers.${TRAEFIK_SERVICE}-adminer.rule=Host(`${DOCKER_DOMAIN}`) && Path(`/adminer`)"
      - "traefik.http.routers.${TRAEFIK_SERVICE}-adminer.service=${TRAEFIK_SERVICE}-adminer"
      - "traefik.http.routers.${TRAEFIK_SERVICE}-adminer.entrypoints=websecure"
      - "traefik.http.routers.${TRAEFIK_SERVICE}-adminer.tls=true"
      - "traefik.http.routers.${TRAEFIK_SERVICE}-adminer.tls.certresolver=le"
      - "traefik.http.services.${TRAEFIK_SERVICE}-adminer.loadbalancer.server.port=8080"
    logging: *default-logging

  media-server:
    restart: unless-stopped
    image: ${MEDIA_SERVER_IMAGE:-ghcr.io/grapeslabs/media-server:1.0.0}
    environment:
      TZ: "Europe/Moscow"
    networks:
      - laravel
      - traefik-public
    expose:
      - 8081
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik-public"
      - "traefik.http.routers.${TRAEFIK_SERVICE}-media.rule=Host(`${DOCKER_DOMAIN}`) && PathPrefix(`/media`)"
      - "traefik.http.routers.${TRAEFIK_SERVICE}-media.entrypoints=websecure"
      - "traefik.http.routers.${TRAEFIK_SERVICE}-media.tls=true"
      - "traefik.http.routers.${TRAEFIK_SERVICE}-media.tls.certresolver=le"
      - "traefik.http.services.${TRAEFIK_SERVICE}-media.loadbalancer.server.port=8081"
      - "traefik.http.middlewares.${TRAEFIK_SERVICE}-media-strip.stripprefix.prefixes=/media"
      - "traefik.http.routers.${TRAEFIK_SERVICE}-media.middlewares=${TRAEFIK_SERVICE}-media-strip"
    volumes:
      - ./media:/data/streams
      - ./media-config.toml:/app/config.toml
    logging: *default-logging

  analytic-database:
    image: pgvector/pgvector:pg15
    environment:
      POSTGRES_USER: ${ADB_USERNAME}
      POSTGRES_PASSWORD: ${ADB_PASSWORD}
      POSTGRES_DB: ${ADB_DATABASE}
    expose:
      - 5432
    volumes:
      - analytic_database:/var/lib/postgresql/data
      - ./init_db.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - laravel
      - traefik-public
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${ADB_USERNAME}" ]
      interval: 10s
      timeout: 5s
      retries: 5
    logging: *default-logging

  analytic-api:
    image: ${ANALYTIC_API_IMAGE:-ghcr.io/grapeslabs/analytic-api:1.0.0}
    environment:
      - PG_HOST=${ADB_HOST}
      - PG_PORT=${ADB_PORT}
      - PG_USER=${ADB_USERNAME}
      - PG_PASSWORD=${ADB_PASSWORD}
      - PG_DATABASE=${ADB_DATABASE}
      - API_PORT=8082
      - DEBUG_MODE=${APP_DEBUG}
      - PERSON_PHOTOS_PATH=/app/person_photos
      - MIN_WIDTH_PHOTO=${MIN_WIDTH_PHOTO}
      - MOTION_MIN_AREA=${MOTION_MIN_AREA}
      - MOTION_THRESHOLD=${MOTION_THRESHOLD}
      - MOTION_RECORD_AFTER_TIME=${MOTION_RECORD_AFTER_TIME}
      - TZ=Europe/Moscow
    expose:
      - 8082
    volumes:
      - ./analytic/person-photos:/app/person_photos
      - ./analytic/thumbnails:/app/thumbnails
    depends_on:
      analytic-database:
        condition: service_healthy
    networks:
      - laravel
    logging: *default-logging

  analytic-server:
    image: ${ANALYTIC_SERVER_IMAGE:-ghcr.io/grapeslabs/analytic:1.0.0}
    environment:
      - PG_HOST=${ADB_HOST}
      - PG_PORT=${ADB_PORT}
      - PG_USER=${ADB_USERNAME}
      - PG_PASSWORD=${ADB_PASSWORD}
      - PG_DATABASE=${ADB_DATABASE}
      - PACS_API_URL=http://analytic-api:8082
      - THUMBNAIL_PATH=/app/thumbnails
      - DEBUG_MODE=${APP_DEBUG}
      - MIN_WIDTH_PHOTO=${MIN_WIDTH_PHOTO}
      - EUCLIDEAN_THRESHOLD=${EUCLIDEAN_THRESHOLD}
      - MAX_FACES_IN_LIST=${MAX_FACES_IN_LIST}
      - CAMERA_POLL_INTERVAL=${CAMERA_POLL_INTERVAL}
      - TZ=Europe/Moscow
    volumes:
      - ./test:/app/captured_images
      - ./analytic/thumbnails:/app/thumbnails
    depends_on:
      analytic-api:
        condition: service_started
    networks:
      - laravel
    logging: *default-logging

  grz-database:
    image: pgvector/pgvector:pg15
    environment:
      POSTGRES_USER: ${GRZ_DB_USERNAME}
      POSTGRES_PASSWORD: ${GRZ_DB_PASSWORD}
      POSTGRES_DB: ${GRZ_DB_DATABASE}
    expose:
      - 5432
    volumes:
      - grz_database:/var/lib/postgresql/data
      - ./grz/init_db.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - laravel
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${GRZ_DB_USERNAME}"]
      interval: 10s
      timeout: 5s
      retries: 5
    logging: *default-logging

  grz-api:
    image: ${GRZ_API_IMAGE:-grz-api:local}
    restart: unless-stopped
    environment:
      - PG_HOST=${GRZ_DB_HOST}
      - PG_PORT=${GRZ_DB_PORT}
      - PG_USER=${GRZ_DB_USERNAME}
      - PG_PASSWORD=${GRZ_DB_PASSWORD}
      - PG_DATABASE=${GRZ_DB_DATABASE}
      - TZ=Europe/Moscow
      - MODE=pacs
    expose:
      - 5000
    volumes:
      - ./grz/logs:/app/logs
    depends_on:
      grz-database:
        condition: service_healthy
    networks:
      - laravel
    logging: *default-logging

  grz-core:
    image: ${GRZ_CORE_IMAGE:-grz-core:local}
    restart: unless-stopped
    environment:
      - PG_HOST=${GRZ_DB_HOST}
      - PG_PORT=${GRZ_DB_PORT}
      - PG_USER=${GRZ_DB_USERNAME}
      - PG_PASSWORD=${GRZ_DB_PASSWORD}
      - PG_DATABASE=${GRZ_DB_DATABASE}
      - PACS_API_URL=http://grz-api:5000
      - TZ=Europe/Moscow
      - MODE=pacs
    volumes:
      - ./grz/events:/app/events
      - ./grz/logs:/app/logs
    depends_on:
      grz-database:
        condition: service_healthy
        required: false
    networks:
      - laravel
    logging: *default-logging

  events-processor:
    build:
      context: ./docker/php
      dockerfile: Dockerfile
      target: app
      args:
        - UID=${UID:-1000}
        - GID=${GID:-1000}
    restart: unless-stopped
    volumes:
      - ./:/var/www/html:delegated
    depends_on:
      - app-database
      - redis
    environment:
      TZ: Europe/Moscow
    networks:
      - laravel
    entrypoint: [ "php", "/var/www/html/artisan", "events:process-new" ]
    logging: *default-logging
  grz-rules-processor:
    build:
      context: ./docker/php
      dockerfile: Dockerfile
      target: app
      args:
        - UID=${UID:-1000}
        - GID=${GID:-1000}
    restart: unless-stopped
    volumes:
      - ./:/var/www/html:delegated
    depends_on:
      - app-database
      - redis
    environment:
      TZ: Europe/Moscow
    networks:
      - laravel
    entrypoint: [ "php", "/var/www/html/artisan", "grz:process-rules" ]
    logging: *default-logging

  ofelia:
    image: mcuadros/ofelia:latest
    restart: unless-stopped
    command: daemon --docker
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    logging: *default-logging

networks:
  laravel:
  traefik-public:
    name: "traefik-public"
    external: true

volumes:
  app_database:
  analytic_database:
  grz_database:
  redis_data:
